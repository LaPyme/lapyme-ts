/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: d8ab02ca40e5
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type Warehouse = {
  id: string;
  name: string;
  address: string | null;
  isDefault: boolean;
  isActive: boolean;
};

/** @internal */
export const Warehouse$inboundSchema: z.ZodMiniType<Warehouse, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    address: types.nullable(types.string()),
    isDefault: types.boolean(),
    isActive: types.boolean(),
  });

export function warehouseFromJSON(
  jsonString: string,
): SafeParseResult<Warehouse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Warehouse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Warehouse' from JSON`,
  );
}
