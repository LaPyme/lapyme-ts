/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type Customer = {
  id: string;
  name: string;
  email: string | null;
  phone: string | null;
  taxId: string | null;
  taxIdType: string | null;
  taxCategory: string | null;
  address: string | null;
  apartment: string | null;
  city: string | null;
  province: string | null;
  isActive: boolean | null;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const Customer$inboundSchema: z.ZodMiniType<Customer, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    email: types.nullable(types.string()),
    phone: types.nullable(types.string()),
    taxId: types.nullable(types.string()),
    taxIdType: types.nullable(types.string()),
    taxCategory: types.nullable(types.string()),
    address: types.nullable(types.string()),
    apartment: types.nullable(types.string()),
    city: types.nullable(types.string()),
    province: types.nullable(types.string()),
    isActive: types.nullable(types.boolean()),
    createdAt: types.date(),
    updatedAt: types.date(),
  });

export function customerFromJSON(
  jsonString: string,
): SafeParseResult<Customer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Customer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Customer' from JSON`,
  );
}
