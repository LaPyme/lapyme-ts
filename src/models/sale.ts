/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdk-validation-error.js";

export type SaleCustomer = {
  id: string;
  name: string;
  email: string | null;
  phone: string | null;
  taxId: string | null;
  address: string | null;
  apartment: string | null;
  city: string | null;
  province: string | null;
};

export type SaleProduct = {
  id: string;
  name: string;
  sku: string;
};

export type SaleWarehouse = {
  id: string;
  name: string;
};

export type SaleItem = {
  id: string;
  quantity: number;
  unitPrice: number;
  unitCost: number;
  taxRateId: number;
  discountPercentage: string | null;
  product: SaleProduct;
  warehouse: SaleWarehouse;
};

export type Sale = {
  id: string;
  invoiceNumber: number | null;
  formattedInvoiceNumber: string | null;
  invoiceDate: Date;
  dueDate: Date;
  serviceFrom: Date | null;
  serviceTo: Date | null;
  currency: string;
  subtotal: number;
  taxAmount: number;
  total: number;
  balance: number | null;
  concept: number | null;
  notes: string | null;
  fiscalNotes: string | null;
  integrationSource: string | null;
  integrationId: string | null;
  customer: SaleCustomer | null;
  items: Array<SaleItem>;
  createdAt: Date;
  updatedAt: Date;
};

/** @internal */
export const SaleCustomer$inboundSchema: z.ZodMiniType<SaleCustomer, unknown> =
  z.object({
    id: types.string(),
    name: types.string(),
    email: types.nullable(types.string()),
    phone: types.nullable(types.string()),
    taxId: types.nullable(types.string()),
    address: types.nullable(types.string()),
    apartment: types.nullable(types.string()),
    city: types.nullable(types.string()),
    province: types.nullable(types.string()),
  });

export function saleCustomerFromJSON(
  jsonString: string,
): SafeParseResult<SaleCustomer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaleCustomer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaleCustomer' from JSON`,
  );
}

/** @internal */
export const SaleProduct$inboundSchema: z.ZodMiniType<SaleProduct, unknown> = z
  .object({
    id: types.string(),
    name: types.string(),
    sku: types.string(),
  });

export function saleProductFromJSON(
  jsonString: string,
): SafeParseResult<SaleProduct, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaleProduct$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaleProduct' from JSON`,
  );
}

/** @internal */
export const SaleWarehouse$inboundSchema: z.ZodMiniType<
  SaleWarehouse,
  unknown
> = z.object({
  id: types.string(),
  name: types.string(),
});

export function saleWarehouseFromJSON(
  jsonString: string,
): SafeParseResult<SaleWarehouse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaleWarehouse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaleWarehouse' from JSON`,
  );
}

/** @internal */
export const SaleItem$inboundSchema: z.ZodMiniType<SaleItem, unknown> = z
  .object({
    id: types.string(),
    quantity: types.number(),
    unitPrice: types.number(),
    unitCost: types.number(),
    taxRateId: types.number(),
    discountPercentage: types.nullable(types.string()),
    product: z.lazy(() => SaleProduct$inboundSchema),
    warehouse: z.lazy(() => SaleWarehouse$inboundSchema),
  });

export function saleItemFromJSON(
  jsonString: string,
): SafeParseResult<SaleItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SaleItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SaleItem' from JSON`,
  );
}

/** @internal */
export const Sale$inboundSchema: z.ZodMiniType<Sale, unknown> = z.object({
  id: types.string(),
  invoiceNumber: types.nullable(types.number()),
  formattedInvoiceNumber: types.nullable(types.string()),
  invoiceDate: types.date(),
  dueDate: types.date(),
  serviceFrom: types.nullable(types.date()),
  serviceTo: types.nullable(types.date()),
  currency: types.string(),
  subtotal: types.number(),
  taxAmount: types.number(),
  total: types.number(),
  balance: types.nullable(types.number()),
  concept: types.nullable(types.number()),
  notes: types.nullable(types.string()),
  fiscalNotes: types.nullable(types.string()),
  integrationSource: types.nullable(types.string()),
  integrationId: types.nullable(types.string()),
  customer: types.nullable(z.lazy(() => SaleCustomer$inboundSchema)),
  items: z.array(z.lazy(() => SaleItem$inboundSchema)),
  createdAt: types.date(),
  updatedAt: types.date(),
});

export function saleFromJSON(
  jsonString: string,
): SafeParseResult<Sale, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Sale$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Sale' from JSON`,
  );
}
